# Use a base image with Bun preinstalled
FROM oven/bun:latest

WORKDIR /app

# Install dependencies
COPY frontend/bun.lock frontend/package.json ./
RUN bun install

# Copy source (in dev, this is often overridden by a volume)
COPY frontend/ .

EXPOSE 5173

CMD ["bun", "run", "dev", "--host"]
